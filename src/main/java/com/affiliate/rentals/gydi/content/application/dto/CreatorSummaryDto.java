package com.affiliate.rentals.gydi.content.application.dto;

/** Lightweight creator reference used in PropertySocialProofDto. */
public record CreatorSummaryDto(
    Long id,
    String username,
    String avatarUrl
) {}
